<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31"
        android:usesCleartextTraffic="true">

<!--        화면 전환을 위한 엑티피티 지정-->
        <activity android:name=".community"/>
        <activity android:name=".roadmap"/>
        <activity android:name=".login"/>
        <activity android:name=".singup"/>
        <activity android:name=".board_create"/>
        <activity android:name=".chat_create"/>
        <activity android:name=".content"/>
        <activity android:name=".update"/>
        <activity android:name=".android_roadmap"/>
        <activity android:name=".search_content"/>
        <activity android:name=".MainActivity"/>
        <activity android:name=".loading"
            android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
    </application>

</manifest>